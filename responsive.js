// script.js

// Example: Log screen width on resize (optional for debugging responsiveness)
window.addEventListener('resize', () => {
    console.log(`Current screen width: ${window.innerWidth}px`);
});
